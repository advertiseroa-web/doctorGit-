import React from 'react';

// Renders the interactive arrows linking the blocks
export function NodeConnector() {
  return null; // Actual rendering handled by xyflow/react edges internally in the CanvasGrid
}
